import { Montserrat, Caprasimo } from 'next/font/google'

export const textFont = Montserrat({subsets: ['latin'], weight: ['400','600']})

export const tittleFont = Caprasimo({subsets: ['latin'], weight: '400'})