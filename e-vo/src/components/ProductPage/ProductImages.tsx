export default function ProductImages({ images }: {
    images: string[]
}){
    return (
        <ul>{
            // images.map((image, i) => (
            //     <Image src={image}/>
            // ))
        }</ul>
    )
}